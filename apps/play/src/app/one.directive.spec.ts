import { OneDirective } from './one.directive';

describe('OneDirective', () => {
  it('should create an instance', () => {
    const directive = new OneDirective();
    expect(directive).toBeTruthy();
  });
});
